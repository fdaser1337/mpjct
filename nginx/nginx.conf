events {}

http {
    server {
        listen 80;

        location /auth/ {
            proxy_pass http://auth:8080/;
        }

        location /tasks/ {
            proxy_pass http://task:8081/;
        }
    }
}
